'''
When I want to use the data from torch to numpy, I need to do some change.
just like this
Type conversion:
torch:  tensor = torch.FloatTensor(data)
numoy:  data = np.array(data)

ps: use this to change the type
x = torch.ones(100,1)
x.data.numpy()

'''
import numpy as np
import torch

np_data = np.arange(6).reshape((2,3))
torch_data = torch.from_numpy(np_data)

#二者类型转换
tensor2array = torch_data.numpy()

# print(np_data)
# print(torch_data)
# print(tensor2array)

#abs
data = [[1,2],[3,4]]
tensor = torch.FloatTensor(data) #32bit floating point
data = np.array(data)
print(
    '\nnumpy:',data.dot(data),
    '\ntorch:',torch.mm(tensor,tensor)
)


